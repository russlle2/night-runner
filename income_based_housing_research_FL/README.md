# Florida Income-Based Housing Research Workflow

Run `python run_all.py` after installing dependencies from `requirements.txt`.

This README is refreshed by `export_results.py` after the pipeline completes.
